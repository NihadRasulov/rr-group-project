package com.example.project.repository.content.home;

import com.example.project.model.content.home.HomeProjectModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HomeProjectRepository extends JpaRepository<HomeProjectModel,Long> {
}
